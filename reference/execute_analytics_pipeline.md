# Execute the analytics pipeline

Execute the analytics pipeline

## Usage

``` r
execute_analytics_pipeline(raw_data, config)
```

## Arguments

- raw_data:

  List of raw data snapshots

- config:

  Study configuration

## Value

Pipeline results
